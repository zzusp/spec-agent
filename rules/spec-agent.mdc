---
description: 使用 spec-agent 撰写/更新需求文档时的文档顺序、修订记录与澄清约定
globs: spec/**/0[0-4]-*.md
alwaysApply: false
---
# 使用 Spec-agent 时的文档约定

本规则仅适用于 spec-agent 的约定文档（`00-clarifications.md`、`01-analysis.md`、`02-prd.md`、`03-tech.md`、`04-acceptance.md` 及 `spec/00-global-memory.md`）。spec 目录下其他工具生成的文档不适用。

当你通过 spec-agent 插件撰写或更新上述需求文档时，请遵守以下约定。

## 所有 spec-agent 技能范围（强制）

- **spec 目录位置**：`spec/` 位于**用户项目工程根目录**（运行命令时的当前工作目录 / AI IDE 工作区根目录），而非插件/技能所在仓库根目录。
- **仅产出/更新 spec 下内容**：所有 spec-agent 技能（如 `/spec-agent-task`、`/spec-agent-write`、`/spec-agent-clarify`、`/spec-agent-chat`、`/spec-agent-update`、`/spec-agent-memory`、`/spec-agent-check`、`/spec-agent-init`、`/spec-agent-switch`）只创建/更新 `spec/` 下的文件（需求文档、全局记忆、元数据、`.active`、`spec/db/` 等），**禁止**修改项目源代码（如 `.proto`、业务代码、配置文件、依赖等）。
- **临时脚本除外**：为执行约定流程而生成的**临时脚本**（如项目根目录的 `.tmp_inspect_db.py` 用于 DB 探查）允许在需要时创建，**执行完毕后必须删除**；除此之外不得修改工程代码。
- 用户描述的“把某字段从 float 改为 double”等应作为**需求内容**写入 spec 文档；具体代码修改应在文档收敛后、由实现阶段（或用户另行发起）完成。

## 文档顺序与依赖

- 文档撰写顺序固定：**analysis** → **prd** → **tech** → **acceptance**；下游文档必须基于上游内容撰写，不得跳步或颠倒。
- 撰写前纳入：全局记忆（如 `spec/00-global-memory.md`）、已确认的澄清（`00-clarifications.md` 中状态为已确认的项）。
- **acceptance（04-acceptance.md）**：验收项（A-xxx）的「验收步骤」与「通过标准」应表述为可执行、可断言的形式，便于实现阶段按 TDD（RED-GREEN-REFACTOR）将每条验收项转化为先写失败测试再实现。

## 修订记录与签名

- 五份文档（analysis / PRD / tech / acceptance / clarifications）均需包含 **修订记录** 表（修订日期 yyyy-MM-dd、修订人、修订内容摘要）；每次修改任一份文档时，在该文档的修订记录表中追加一行。
- PRD / tech / acceptance 中必须保留 `<!-- DEPENDENCY-SIGNATURE:START --> ... <!-- DEPENDENCY-SIGNATURE:END -->` 区块，且签名与当前上游内容一致（由脚本或流程更新，勿手改破坏）。

## 全局记忆内容约定

- **用途**：`spec/00-global-memory.md` 仅记录**项目级情况**（如技术栈、部署环境、合规要求）与**用户/团队习惯与约定**（如命名规范、交付口径、「所有需求默认记录操作人与来源 IP」等），供各需求文档撰写时引用。
- **禁止**：不得将**单次需求的描述、范围或结论**（如「本需求（xxx）：仅变更某 proto」「本需求不修改业务代码」）写入全局记忆；此类内容属于需求文档或澄清文档（`00-clarifications.md`）。

## 澄清为真源

- 不确定或待产品确认的点必须写入 **00-clarifications.md**；该文件为澄清的真源，`00-clarifications.json` 为机器可读镜像，勿单独手改 json 导致与 md 不一致。
- 应用已确认澄清到正文时：结合澄清内容重设计/调整整份文档，并整体检查矛盾或更优实现；仍需用户确认的项继续作为新行追加到澄清文档。

## 多需求与命令

- 存在多个需求目录时，优先用 `--name` 或 `--path` 显式指定目标需求；若未指定，以 `spec/.active` 指向的为当前需求。
- 撰写或大量修改文档后，按插件说明执行 `sync-memory`、必要时执行 `final-check`，并在未通过时根据结果修订文档直至通过。

---
name: spec-agent-switch
description: Switch active requirement context. Use when user asks to operate on another requirement (by name or path).
---

# Spec agent switch (slash command)

This command invokes the **spec-agent-switch** skill. Follow the full instructions in the skill: `skills/spec-agent-switch/SKILL.md`.

- Resolve target by name or path; run set-active; reply with active name, path, and one next-step suggestion.
- Run from user project root; see AGENTS.md for command contract.

---
name: spec-agent-memory
description: Add or change global memory (project-level conventions, constraints, habits). For in-conversation updates use spec-agent-chat.
---

# Spec agent memory (slash command)

This command invokes the **spec-agent-memory** skill. Follow the full instructions in the skill: `skills/spec-agent-memory/SKILL.md`.

- Read current `spec/00-global-memory.md`; merge new or changed entries; sync snapshot to active requirement metadata after write.
- Run from user project root; see AGENTS.md for command contract.

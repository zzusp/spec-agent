interface:
  display_name: "Spec Agent Chat"
  short_description: "Route chat to clarification or memory and auto-update docs"
  default_prompt: "Use $spec-agent-chat to classify user chat as clarification or memory, pin active requirement name and pass --name on requirement-targeted commands, update docs in subagent stage order using handoff fields from subagent-context (including project_mode/clarification_focus), and rely on final_check failed auto-reopen mapping."

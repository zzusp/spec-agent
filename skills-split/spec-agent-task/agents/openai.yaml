interface:
  display_name: "Spec Agent Task"
  short_description: "AI-first workflow coordinator"
  default_prompt: "Use $spec-agent-task as orchestrator: classify project_mode (greenfield/existing), pass it at init, consume JSON handoff fields from subagent-context, run stage subagents (analysis/prd/tech/acceptance/final_check), commit final_check completed only when issues=0, and use final_check failed auto-reopen mapping."
